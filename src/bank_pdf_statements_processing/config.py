# change these
root_dir = r"C:\...\financial_data"
output_path = r"C:\...\financial_data\output.xlsx"

# # global dictionary storing all the dataframes
# global_df = {}

# column areas for pdf files, in points
column_positions = ['92, 332, 412, 488, 559']

# table area for the first page, in points
table_areas_first_page = ['50, 455, 559, 122']

# table area for the pages after the first and before the last (intermediary pages), in points
table_areas_inbetween_pages = ['50, 706, 559, 122']

# table area for the last page, in points
table_areas_last_page = ['50, 706, 559,122']